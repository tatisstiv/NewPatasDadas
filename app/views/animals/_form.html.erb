<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Nome" %>
    <%= form.text_field :name, id: :animal_name %>
  </div>

  <div class="field">
    <%= form.label :breed, "Raça" %>
    <%= form.text_field :breed, id: :animal_breed %>
  </div>

  <div class="field">
    <%= form.label :color, "Cor" %>
    <%= form.text_field :color, id: :animal_color %>
  </div>

  <div class="field">
    <%= form.label :size, "Tamanho" %>
    <%= form.text_field :size, id: :animal_size %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
