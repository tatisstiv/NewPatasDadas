<%= form_with(model: animal, local: true) do |form| %>
  <% if animal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
      <% animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :animal_nome %>
  </div>

  <div class="field">
    <%= form.label :raca %>
    <%= form.text_field :raca, id: :animal_raca %>
  </div>

  <div class="field">
    <%= form.label :cor %>
    <%= form.text_field :cor, id: :animal_cor %>
  </div>

  <div class="field">
    <%= form.label :tamanho %>
    <%= form.text_field :tamanho, id: :animal_tamanho %>
  </div>

  <div class="field">
    <%= form.label :timestamps %>
    <%= form.text_field :timestamps, id: :animal_timestamps %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
