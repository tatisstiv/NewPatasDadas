<%= form_with(model: fixed_scheduling, local: true) do |form| %>
  <% if fixed_scheduling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fixed_scheduling.errors.count, "error") %> prohibited this fixed_scheduling from being saved:</h2>

      <ul>
      <% fixed_scheduling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day_of_week, "Dia da semana" %>
    <%= form.select :day_of_week, options_for_select(
      [
        ["Segunda-feira", "segunda"],
        ["Terça-feira", "terca"],
        ["Quarta-feira", "quarta"],
        ["Quinta-feira", "quinta"],
        ["Sexta-feira", "sexta"],
        ["Sábado", "sabado"]
      ]
    ) %>
  </div>

  <div class="field">
    <%= form.label :volunteer_id, "Voluntário" %>
    <%= form.text_field :volunteer_id, id: :fixed_scheduling_volunteer_id %>
  </div>

  <div class="field">
    <%= form.label :animal_id, "Animal" %>
    <%= form.text_field :animal_id, id: :fixed_scheduling_animal_id %>
  </div>

  <div class="actions">
    <%= form.submit "Salvar" %>
  </div>
<% end %>
