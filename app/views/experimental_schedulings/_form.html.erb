<%= form_with(model: experimental_scheduling, local: true) do |form| %>
  <% if experimental_scheduling.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(experimental_scheduling.errors.count, "error") %> prohibited this experimental_scheduling from being saved:</h2>

      <ul>
      <% experimental_scheduling.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p>Animal: <%= @experimental_scheduling.animal.name %></p>
  </div>

  <div class="field">
    <%= form.label :date_time, "Data e hora" %>
    <%= form.datetime_select :date_time, id: @experimental_scheduling.date_time %>
  </div>

  <%= form.hidden_field :animal_id, id: @experimental_scheduling.animal_id %>

  <%= form.hidden_field :volunteer_id, id: @experimental_scheduling.volunteer_id %>

  <div class="actions">
    <%= form.submit "Salvar" %>
  </div>
<% end %>
