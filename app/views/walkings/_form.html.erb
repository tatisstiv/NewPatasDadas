<%= form_with(model: walking, local: true) do |form| %>
  <% if walking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(walking.errors.count, "error") %> prohibited this walking from being saved:</h2>

      <ul>
      <% walking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date, "Data" %>
    <%= form.datetime_select :date, id: :walking_date %>
  </div>

  <div class="field">
    <%= form.label :volunteer_id, "Voluntário" %>
    <%= form.collection_select :volunteer_id, Volunteer.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :animal_id, "Animal" %>
    <%= form.collection_select :animal_id, Animal.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit "Salvar" %>
  </div>
<% end %>
