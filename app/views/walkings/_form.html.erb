<%= form_with(model: walking, local: true) do |form| %>
  <% if walking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(walking.errors.count, "error") %> prohibited this walking from being saved:</h2>

      <ul>
      <% walking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date, id: :walking_date %>
  </div>

  <div class="field">
    <%= form.label :volunteer_id %>
    <%= form.text_field :volunteer_id, id: :walking_volunteer_id %>
  </div>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.text_field :animal_id, id: :walking_animal_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
