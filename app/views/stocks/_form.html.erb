<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Nome" %>
    <%= form.text_field :name, id: :stock_name %>
  </div>

  <div class="field">
    <%= form.label :amount, "Quantidade" %>
    <%= form.text_field :amount, id: :stock_amount %>
  </div>

  <div class="field">
    <%= form.label :kind , "Tipo" %>
    <%= form.text_field :kind, id: :stock_kind %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
